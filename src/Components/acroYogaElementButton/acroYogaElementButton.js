import React from 'react'

export default function acroYogaElementButton(props) {
    let {id, name} = this.props;

    return (
        <button key={id}>
                {name}
        </button>
    )
}
